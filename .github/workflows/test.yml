name: Test artifacts

on:
  workflow_dispatch:
    inputs:
      branch_name:
        description: "Branch to run the workflow on"
        required: true
        default: "master"

jobs:
  test-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Building unit tests
        run: |
          cd test/unit
          make all
      - name: Doing unit test scenarios
        run: |
          cd test/unit
          ./test
      - name: Doing integration test scenarios
        run: |
          cd test/integration
          ./test.sh
