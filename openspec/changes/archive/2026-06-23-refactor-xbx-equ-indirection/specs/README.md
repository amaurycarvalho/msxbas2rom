# No specs for this change

This change is purely internal refactoring (redirecting XBASIC_* EQUs from hardcoded addresses to native labels). It introduces no new capabilities and modifies no existing requirement behavior. All external interfaces, symbol exports, dispatch table resolution, and runtime behavior remain unchanged.

See `proposal.md` for motivation and `design.md` for technical approach.
