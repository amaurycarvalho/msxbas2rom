# No specs for this change

This change is purely internal code maintenance (disabling unreachable dead code in the Z80 assembly support layer). It introduces no new capabilities and modifies no existing requirement behavior. All external interfaces, symbol exports, and runtime behavior remain unchanged.

See `proposal.md` for motivation and `design.md` for technical approach.
