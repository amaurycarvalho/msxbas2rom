#include "compiler_defsng_statement_strategy.h"

#include "compiler_context.h"

bool CompilerDefsngStatementStrategy::execute(
    shared_ptr<CompilerContext> context) {
  return true;  // resolved by parser
}
